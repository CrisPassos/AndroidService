<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rm31456.androidservice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".receiver.AlarmReceiver" />

        <activity android:name=".Atividade2Activity" />
        <activity android:name=".MainActivity" />

        <service
            android:name=".services.Logger"
            android:icon="@mipmap/ic_launcher"
            android:label="Serviço de Mensagens" />

        <activity android:name=".Atividade3Activity"></activity>

        <service android:name=".services.BoundService"/>
    </application>

</manifest>